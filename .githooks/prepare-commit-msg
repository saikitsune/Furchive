#!/bin/sh
# Wrapper to call the PowerShell prepare-commit-msg hook on Windows/Git Bash
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd -W 2>/dev/null || pwd)"
exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$SCRIPT_DIR\prepare-commit-msg.ps1" "$@"
