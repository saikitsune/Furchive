<Application x:Class="Furchive.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:local="clr-namespace:Furchive.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemeResources />
                <ui:XamlControlsResources />
            </ResourceDictionary.MergedDictionaries>

            <!-- Value Converters -->
            <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
            <local:NullToBooleanConverter x:Key="NullToBooleanConverter"/>
            <local:BytesToStringConverter x:Key="BytesToStringConverter"/>
            <local:UppercaseConverter x:Key="UppercaseConverter"/>
            <local:StringEqualsConverter x:Key="StringEqualsConverter"/>
            <local:ImageCacheConverter x:Key="ImageCacheConverter"/>
            <local:IsMediaDownloadedConverter x:Key="IsMediaDownloadedConverter"/>
            <local:CategoryKeyToHeaderConverter x:Key="CategoryKeyToHeaderConverter"/>
            <local:FilterAndOrderCategoriesConverter x:Key="FilterAndOrderCategoriesConverter"/>
            <local:CategoryToBrushConverter x:Key="CategoryToBrushConverter"/>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <!-- Global Styles -->
            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
            </Style>

            <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
            </Style>

            <Style x:Key="ToolbarButtonStyle" TargetType="Button">
                <Setter Property="Margin" Value="2"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="MinWidth" Value="64"/>
                <Setter Property="Height" Value="30"/>
            </Style>

            <Style x:Key="TagButtonStyle" TargetType="Button">
                <Setter Property="Margin" Value="3,2"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundBaseLowBrush}"/>
                <!-- Use a TextBlock as content to avoid underscore being treated as an access key -->
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
